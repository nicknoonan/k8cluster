name: Build & Push Minecraft Bot

on:
  push:
    branches:
      - main
    paths:
      - 'minecraft/bot/Dockerfile'
      - 'minecraft/bot/keys.json'
      - 'minecraft/bot/profiles/**'
  workflow_dispatch:   # allow manual triggering

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: nicknoonan/minecraft-bot

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # tag with git SHA for immutable rollbacks
            type=sha,prefix=sha-,format=short
            # always update :latest on main
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: minecraft/bot
          file: minecraft/bot/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
