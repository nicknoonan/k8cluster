# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: alloy
#   namespace: argocd
# spec:
#   project: default
#   source:
#     repoURL: 'https://grafana.github.io/helm-charts'
#     chart: alloy
#     targetRevision: 0.x.x
#     helm:
#       values: |
#         alloy:
#           configMap:
#             content: |
#               // Configure Alloy pipeline to forward logs to Loki, 
#               // traces to Tempo, metrics to Prometheus, and profiles to Pyroscope.
#   destination:
#     server: 'https://kubernetes.default.svc'
#     namespace: monitoring
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true